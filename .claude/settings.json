{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "postToolUse": [
      {
        "pattern": "Edit(file_path:**/index.js)",
        "command": "npm test 2>/dev/null || echo '⚠️  No tests configured yet. Run /test-mcp-tools to create tests.'",
        "description": "Run tests after editing index.js (once tests exist)"
      }
    ],
    "preToolUse": [
      {
        "pattern": "Edit(file_path:**/package-lock.json)",
        "command": "echo '❌ Use npm install/update instead of editing package-lock.json directly' && exit 1",
        "description": "Prevent manual edits to package-lock.json"
      }
    ]
  }
}
